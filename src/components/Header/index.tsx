import UserName from "../UserName"
import Navigation from "../Navigation"

const Header = () => {
    return(
        <header>
            <UserName/>
            <Navigation/>
        </header>
    )
}

export default Header