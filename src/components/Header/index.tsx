import UserName from "../UserName"
import Logo from "../Logo"

const Header = () => {
    return(
        <header>
            <Logo/>
            <UserName/>
        </header>
    )
}

export default Header